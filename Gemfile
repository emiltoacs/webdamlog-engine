# This is used by bundler to setup the gems dependencies:
# http://gembundler.com/
source 'https://rubygems.org'

gem 'bud', '>= 0.9.7'
gem 'polyglot'
gem 'treetop'
# yaml and prettyprint are now std-lib in ruby
#gem 'yaml'
#gem 'prettyprint'
group :development, :test do
  gem 'debase'
  gem 'ruby-debug-ide', '>= 0.4.17'
end

ruby '2.0.0'